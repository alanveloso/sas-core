# Evidência — P6 GATE VERIFY (Fase 6 em curso)

**Data:** 2026-08-07 (~15:47–15:50 local / 18:47–18:50Z)  
**Branch:** `feat/p6-protection-models`  
**HEAD:** `6bdb370` + working tree (P6-001 packaging + residual P5 FAD/matrix)  
**Comando:** `/verify-current-phase`  
**Artefactos:** `artifacts/winnforum/p6_gate_verify_20260807T185000Z/`

## Resultado do gate

**FASE 6: NÃO APROVADA** (fase em curso; só P6-001 concluída)

Critérios do plano (`Gate da fase 6`):

| Critério | Estado |
|---|---|
| Modelos numéricos passam tolerâncias do reference implementation | **FAIL** — P6-002/P6-003 ausentes |
| IAP determinístico | **FAIL** — P6-004 ausente |
| Famílias avançadas Release 1 verdes (GPR/PCR/PAT/PPR/FPR/IPR/MCP + FDB RF) | **FAIL** — ainda blocked/failing/untested |

### Progresso por task

| Task | Resultado |
|---|---|
| P6-001 Empacotar modelos e dados | **PASS** (local) |
| P6-002 HAAT geral | **PENDENTE** |
| P6-003 Propagation Model API | **PENDENTE** |
| P6-004 IAP / aggregate interference | **PENDENTE** |
| P6-005 Fechar famílias avançadas | **PENDENTE** |
| **P6 overall** | **FAIL** (gate incompleto) |

---

## Parte A — Checks locais (output real)

| Check | Comando | Resultado |
|---|---|---|
| Full pytest (1ª) | `.venv/bin/python -m pytest -q` | **FAIL** `4 failed, 571 passed, 7 skipped` — matrix schema |
| Matrix pós-fix | `.venv/bin/python -m pytest -q tests/unit/test_compliance_matrix.py` | **12 passed** |
| Full pytest (rerun) | `.venv/bin/python -m pytest -q` | **575 passed, 7 skipped** |
| Ruff | `.venv/bin/ruff check .` | **All checks passed!** |
| P6-001 + doctor + startup | `pytest …/test_protection_data.py …/test_certs_and_doctor.py …/test_startup.py` | **25 passed** |
| HAAT unit | `pytest …/test_haat_registration.py …/test_registration_no_fixture_hardcode.py` | **17 passed** |
| Doctor | `.venv/bin/python -m tools.doctor` | **FAIL** ambiental (`certs/` ausente); `protection_data` **OK** (soft gap `dpa_payload`) |

### Bundle protection_data (smoke)

`cbrs_winnforum_protection` v1.0.0, `strict=False` → `ok=True`, slots=11, soft gap: `dpa_payload` (sem `*.kml`).

---

## Parte B — Defeito encontrado e correção

### Causa raiz (produto / compliance matrix)

`FAMILY.FAD` estava `status: passing` com `case_scope: family`, o que o schema proíbe
(`case_scope=family cannot be status=passing`). Introduzido no working tree do P5 gate
final; quebrava `tests/unit/test_compliance_matrix.py` (4 testes).

### Fix mínimo

1. `FAMILY.FAD` → `status: failing` (rollup, padrão SSS).
2. Casos `WINNF.FT.S.FAD.1` e `WINNF.FT.S.FAD.2` → `passing` com
   `evidence: compliance/evidence/P5_GATE_FINAL.md`.
3. `FAMILY.PAT` implementation: `protection_data/` (dir) → ficheiros
   `protection_data/loader.py` + `schema.py` (schema exige `is_file()`).

Nenhum algoritmo RF/IAP alterado nesta verificação.

---

## Parte C — Oficial / harness

Harness disponível em `../winnforum-sas-harness`.

**Não executado** neste verify como gate de aprovação P6:

- FDB / IPR / PAT / MCP RF — ainda **BLOCKED_BY_P6** por física/IAP (P6-002…P6-005);
- corrê-los agora só reconfirmaria falha de produto já classificada, sem critério de
  gate cumprido.

Não se inventou PASS oficial.

---

## Parte D — Riscos e próximo passo

- Working tree mistura residual P5 (FAD client + `P5_GATE_FINAL`) com P6-001.
- Payload DPA KML ausente; strict mode falharia doctor/startup.
- Propagation Admin API continua 501.
- `_KNOWN_STREET_HAAT` já ausente no código (anti-hardcode verde); P6-002 ainda exige
  HAAT geral com amostras/tolerâncias documentadas além do caminho NED parcial.

**Próxima task:** **P6-002 — HAAT geral**.
