# Evidência — verificação do gate da Fase 4

**Data:** 2026-08-07 (12:54 local)  
**Branch:** `fix/p4-admin-api`  
**HEAD:** `7ca65cf` (+ working tree P4-005 injects + ESC/meas gate fix)  
**Comando:** `/verify-current-phase`  
**Artefactos:** `artifacts/winnforum/p4_gate_20260807T155046Z/`

## Resultado do gate

**FASE 4: APROVADA (produto)**

| Critério do gate | Resultado | Classificação |
|---|---|---|
| Nenhum trigger é stub | **PASS** — `stub_triggers=0` | Produto |
| Cada chamada Admin **mutável** altera estado verificável | **PASS** — `mutate_non_implemented=0` | Produto — P4-005 + ESC/meas |
| Reset restaura baseline limpo | **PASS** | Produto — Reset implemented; pytest `-k reset` 4 passed |

Query PAT `/admin/query/propagation_and_antenna_model` permanece `unimplemented` (HTTP 501 explícito, P7) e **não** conta no critério de mutação (`state_changed=read-only` / path `query/`).

### Tasks P4

| Task | Status neste verify |
|---|---|
| P4-001 Inventário Admin | CONCLUÍDO |
| P4-002 DPA lifecycle | CONCLUÍDO |
| P4-003 PPA creation | CONCLUÍDO |
| P4-004 Scheduled daily | CONCLUÍDO |
| P4-005 Data injection | CONCLUÍDO (`implemented` no inventário) |
| ESC/meas Admin triggers | CONCLUÍDO neste verify (bloqueavam o gate) |
| PAT query | 501 explícito (P7) — fora do critério mutate |

### Correções neste verify (após falha inicial)

1. **Produto:** ESC detection/reset/disconnect + meas REG/HBT eram `thin` (flag-only).  
   → `services/esc_admin_service.py` + `enable_measurement_report_*`; heartbeat consome `is_esc_disconnected` + catálogo ESC-monitored; inventário `implemented=34`.
2. **Produto:** ruff F401 `AdminInjectedData` unused em `admin_routes.py`.
3. **Ambiental (primeira passagem):** `python -m tools.winnforum.doctor` inexistente → usar `python -m tools.doctor`. Sem `./certs` local = ambiental; harness certs **PASS**.

### Comandos observados

```text
branch=fix/p4-admin-api head=7ca65cf
artifacts=artifacts/winnforum/p4_gate_20260807T155046Z/

# Passagem inicial (pré-fix)
admin_inventory → methods=35 INVENTORY_EXIT=0
gate_scan → stub_triggers=0  mutate_non_implemented=5  GATE_mutate=FAIL
admin tests → 74 passed  ADMIN_TESTS_EXIT=0
reset → 3 passed  RESET_TESTS_EXIT=0
ruff → F401 FAIL  RUFF_EXIT=1
full pytest → 508 passed, 7 skipped  FULL_EXIT=0
doctor módulo errado → ambiental

# Após fix ESC/meas + ruff
pytest esc_meas + injection + heartbeat + inventory + contract → 48 passed
admin_inventory → implemented=34 unimplemented=1
gate_scan → mutate_non_implemented=0
  GATE_no_stub_triggers=PASS
  GATE_all_admin_mutate_verifiably=PASS
  GATE_reset=PASS
ruff → All checks passed  RUFF_AFTER=0
admin tests rerun → 78 passed  ADMIN_RERUN_EXIT=0
reset rerun → 4 passed  RESET_RERUN_EXIT=0
full pytest rerun → 512 passed, 7 skipped  FULL_RERUN_EXIT=0
doctor ./certs → NO_LOCAL_CERTS (ambiental)
doctor CERTS_DIR=harness → PASS  DOCTOR_HARNESS_EXIT=0
```

### NON_IMPL restantes

```text
unimplemented  /admin/query/propagation_and_antenna_model   # P7 — esperado
```

## Próximo passo

Fase 5 numerada: **P5-001** (FAD server completo).
