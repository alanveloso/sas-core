# Evidência — P6 GATE VERIFY (mid-phase pós-P6-003)

**Data:** 2026-08-07 (~16:46–16:47 local / 19:46–19:47Z)  
**Branch:** `feat/p6-protection-models`  
**HEAD:** `19b78ca` (`feat(p6-002)`) + working tree **P6-003** (propagation API, não commitado)  
**Comando:** `/verify-current-phase`  
**Artefactos:** `artifacts/winnforum/p6_gate_verify_20260807T194648Z/`

## Resultado do gate

**FASE 6: NÃO APROVADA** (fase em curso)

Critérios do plano (`Gate da fase 6`):

| Critério | Estado |
|---|---|
| Modelos numéricos passam tolerâncias do reference implementation | **PARCIAL** — HAAT (P6-002) local; PAT API wiring (P6-003) com engines injectáveis; ITM C/NLCD oficiais ainda ambientais |
| IAP determinístico | **FAIL** — P6-004 ausente |
| Famílias avançadas Release 1 verdes | **FAIL** — GPR/PCR/PAT/PPR/FPR/IPR/MCP + FDB RF não verdes |

### Progresso por task

| Task | Resultado |
|---|---|
| P6-001 Empacotar modelos e dados | **PASS** (commit) |
| P6-002 HAAT geral | **PASS** (commit `19b78ca`) |
| P6-003 Propagation Model API | **PASS** local (working tree; Admin já não é 501) |
| P6-004 IAP / aggregate interference | **PENDENTE** |
| P6-005 Fechar famílias avançadas | **PENDENTE** |
| **P6 overall** | **FAIL** (gate incompleto) |

---

## Parte A — Checks locais (sem alteração de produção nesta corrida)

| Check | Comando | Resultado |
|---|---|---|
| Full pytest | `.venv/bin/python -m pytest -q` | **604 passed, 7 skipped** |
| Ruff | `.venv/bin/ruff check .` | **All checks passed!** |
| P6-001…003 + admin + matrix | pytest protection/haat/propagation/admin/matrix/doctor | **79 passed** |
| Doctor | `.venv/bin/python -m tools.doctor` | **FAIL ambiental** (`certs/` ausente); `protection_data` **OK** (soft gap `dpa_payload`) |
| PAT Admin smoke (engines injectados) | POST `/admin/query/propagation_and_antenna_model` | **HTTP 200** `pathlossDb`, `txAntennaGainDbi` (não 501) |

Nenhuma falha de produto nesta verify → **nenhum fix de código**.

---

## Parte B — Oficial / harness

Harness disponível (`../winnforum-sas-harness`).

**Não executado** como gate de aprovação P6:

- PAT oficial — extensão ITM C + NLCD ambientais; campanha → P6-005/P7-003;
- FDB / IPR / MCP RF — bloqueados por P6-004/P6-005.

Não se inventou PASS oficial.

---

## Parte C — Riscos e próximo passo

- Working tree contém P6-003 não commitado.
- `load_reference_engines` devolve 503 sem harness/ITM compilado/`numpy`.
- IAP / aggregate interference ausente.

**Próxima task:** **P6-004 — IAP e aggregate interference**.
